// الأس التكعيبي

// قم بكتابة الدالة function تستقبل عدد صحيح integer. تقوم الدالة بإرجاع الأُس التكعيبي exponent Cube لهذا العدد.


function exponent_cube(number) {
    return Math.pow(number, 3);
}
